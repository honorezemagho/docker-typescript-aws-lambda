# Server Configuration
PORT=3010
HOST=0.0.0.0
NODE_ENV=development

# Application Settings
APP_NAME=Lambda Server
APP_VERSION=1.0.0

# Logging
LOG_LEVEL=info

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=*

# Timeouts (in milliseconds)
REQUEST_TIMEOUT=30000
KEEP_ALIVE_TIMEOUT=5000
