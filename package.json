{
  "name": "lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function with TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm run build && zip -r function.zip ."
  },
  "keywords": ["aws", "lambda", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/dotenv": "^6.1.1",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
